export const localSetItem = (itemName, value) => localStorage.setItem(itemName, value);
export const localGetItem = (itemName) => localStorage.getItem(itemName);
export const localRemoveItem = (itemName) => localStorage.removeItem(itemName);
export const localClear = () => localStorage.clear();
